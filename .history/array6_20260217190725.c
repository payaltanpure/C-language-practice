#include <stdio.h>
int main()
{
    int arr[10];
    int i, min, max, avg,sum=0;

    printf("\nEnter any 10 elemts in array:");
    for (i=0; i<10;i++)
    {
        scanf("%d", &arr[i]);
    }

    min = arr[0];
    max=ar

    return 0;
}