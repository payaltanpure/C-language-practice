#include <stdio.h>
void circlearea();
void triarea();
int main()
{
   int ch;
   printf("\n1.Area of Circle \n2.Area of triangle \n 3.Area of rectangle\n 4. Exit");
   scanf("%d", &ch);
   switch(ch)
   {
      case 1:
      void circlearea();
      break;

      case 1:
      void circlearea();
      break;
   }
   return 0;
}
