#include <stdio.h>
int main()
{
    int n1= 10;
    int n2= 20;
    int add;

    int *p
    printf("\nValue of n is:%d",n);
    printf("\nAddress of n is:%u",&n);
 
    printf("\nValue of n using pointer is:%d",*ptr);
    printf("\nAddress of using pointer n is:%u",ptr);

    return 0;
}