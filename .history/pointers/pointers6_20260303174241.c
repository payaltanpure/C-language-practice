#include <stdio.h>
void createarray(int);
int main()
{
    int no;
    printf("\nEnter size of array:");
    scanf("%d", &no);
    createarray(no);
    return 0;
}
void createarray(int l)
{
    for(int i=0; i<l;i++)
    {
        printf("\nEnter the array elemnts:");
        
    }
}