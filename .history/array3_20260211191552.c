#include <stdio.h>
int main()
{
    int arr[5];
    int i, n ;
    printf("\nEnter the no to be searched:");
    printf("\nTake user input:");
    for(i=0;i<10; i++)
    {
        scanf("%d", &arr[i]);
        if(arr[i]%2==0)
        {
          
        }
        else{
            
        }
    }

    return 0;
}