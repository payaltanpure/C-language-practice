#include <stdio.h>
int main()
{
    int arr[10];
    int n;
    prinrintf("\nEnter the no of elemnets to put in array:");
    scanf("%d", &n);
    printf("\nenter array elemnts:");
    for (int i=0; i)
    return 0;
}