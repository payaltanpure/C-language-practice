#include <stdio.h>
int main()
{
    int arr[5];
    int i, n , flag
    
    printf("\nTake user input:");
    for(i=0;i<10; i++)
    {
        scanf("%d", &arr[i]);
        if(arr[i]==n)
        {
           printf("\nNumber %d is found in array", arr[i]);
        }
        else{
            
        }
    }
    printf("\nEnter the no to be searched:");
    scanf("%d", &n);
    return 0;
}