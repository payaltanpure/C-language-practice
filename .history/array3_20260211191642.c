#include <stdio.h>
int main()
{
    int arr[5];
    int i, n ;
    printf("\nEnter the no to be searched:");
    scanf("%d", &n);
    printf("\nTake user input:");
    for(i=0;i<10; i++)
    {
        scanf("%d", &arr[i]);
        if(arr[i]==n)
        {
           printf("\nNumber ")
        }
        else{
            
        }
    }

    return 0;
}