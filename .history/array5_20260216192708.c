#include <stdio.h>
int main()
{
    int arr[10];
    int n;
    printf("\nMenu:");
    prinf("\n1.Creating array \n 2.")
    prinrintf("\nEnter the no of elemnets to put in array:");
    scanf("%d", &n);
    printf("\nenter array elemnts:");
    for (int i=0; i<n; i++){
        scanf("%d", &arr[i]);
    }

    return 0;
}